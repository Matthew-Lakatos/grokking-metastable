# load grid of configs (list of dicts)
# for each config:
#   for seed in seeds:
#       call run_experiment.main(config with seed) or import train() and run in-process
#       collect tau_grok and path to CSV
# aggregate results into a master CSV for analysis
